.data
.text

main:  # O ponto de entrada do programa
    addi $t0, $zero, 10   # $t0 = 0 + 10;  O registrador $t0 recebe o valor 10.
    addi $t1, $t0, 20    # $t1 = $t0 + 20; O registrador $t1 recebe o resultado da soma de $t0 e 20.

